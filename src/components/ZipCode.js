import React from "react"

function ZipCode({ zip }) {

    // const timestamp = new Date(comment.Created.toDate())
    // const tsMonth = timestamp.getMonth() + 1
    // const tsDay = timestamp.getDate()
    // const tsYear = timestamp.getFullYear()
    return (
        {/* <li class="list-group-item">
            <p><img class="rounded-circle" src={"https://api.adorable.io/avatars/120/" + comment.Name + ".png"} alt={comment.Name} /> <em>{comment.Name} commented on {tsMonth}/{tsDay}/{tsYear}</em></p>
            <p>{comment.Message}</p>
        </li> */}
    )
}

export default ZipCode